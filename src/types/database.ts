// Se regenera con `supabase gen types typescript` una vez el proyecto de Supabase
// tenga docs/SCHEMA.sql aplicado (ver ARCHITECTURE.md, recomendación de la sección 1.2).
export type Database = Record<string, never>;
